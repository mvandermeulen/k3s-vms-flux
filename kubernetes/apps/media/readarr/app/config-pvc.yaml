---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: readarr-config-0
  namespace: media
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
  storageClassName: ceph-block
